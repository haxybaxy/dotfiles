# tmuxinator project: frontend
# Start with: tmuxinator start frontend   (also auto-used by tmux-sessionizer)
# Managed in dotfiles via stow (package: tmuxinator) -> ~/.config/tmuxinator/frontend.yml

name: frontend
root: ~/work/skoll/frontend

windows:
  - shell:
  - editor: nvim
  - herdr: herdr session attach frontend
