# tmuxinator project: infra
# Start with: tmuxinator start infra   (also auto-used by tmux-sessionizer)
# Managed in dotfiles via stow (package: tmuxinator) -> ~/.config/tmuxinator/infra.yml

name: infra
root: ~/work/skoll/infra

windows:
  - shell:
  - editor: nvim
  - herdr: herdr session attach infra
